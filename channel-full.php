<?php
// Раньше здесь был точный дубликат channel.php (900 строк один в один) — расходились
// при правках и путали, какой файл на самом деле обновлять. Теперь это просто редирект,
// чтобы старые ссылки на channel-full.php не превращались в 404.
$slug = $_GET['slug'] ?? '';
header('Location: /channel.php' . ($slug ? '?slug=' . urlencode($slug) : ''), true, 301);
exit;
