# Если в .htaccess есть Header set Strict-Transport-Security — УДАЛИ эти строки.
# Они ломают телефон при SSL-проблемах InfinityFree.
# Пример что удалить:
# Header always set Strict-Transport-Security "max-age=..."
