-- Номер телефона — обязательное поле (запасной способ входа, если забыли почту).
-- Без SMS-подтверждения (по требованию), только проверка формата на реальный номер.
ALTER TABLE users ADD COLUMN phone VARCHAR(20) DEFAULT NULL UNIQUE;
